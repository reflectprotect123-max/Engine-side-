{
  "name": "engineside",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "node scripts/bundle-adaptive.mjs",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test && node --test engine.test.js library.test.js session.test.js supabase/functions/_shared/auth-exports.test.js",
    "verify": "pnpm run typecheck && pnpm run test && pnpm run build && node checks/engine-app.smoke.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
